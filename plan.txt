PRE REQUISITES

1. Previous day's transaction report
2. Previous day's Overall report
3. today's file (trnsc_id, acc_no, type_of_transac, amount)
    Type - 
    1. deposit = +balance (trnsc_id, type_of_transac, source_acc_no,  amount)
    2. withdraw = -balance (trnsc_id, type_of_transac, source_acc_no,  amount)
    3. transfer = -balance (trnsc_id, type_of_transac, source_acc_no, tranfered_to, amount)
                  +balance (tranfered to)

ACTIONS TO PERFORM

1. Display Daily report

    1. Total number of transaction requests
    2. NO. of succesfull transaction
    3. No. of failed transaction
    4. Total number of deposits
    5. Total amount deposits in a day
    6. Total no of withdrawls
    7. Total withdrawls in a day
    8. Net Amount in Bank
    9. Change in a day

2. Display comparison between Daily report's of today and previous day

3. Create today's overall report in .csv file

STORAGE

1. Individual account information and operations (CLASS)

class BankAccount {
    acc_no = 
    name = 
    __balance = 

    Methods:

    deposit()
    withdraw()
    transfer()
    receive()
}

2. All accounts (DICTIONARY)

    accounts = {"acc_no" : BankAccount(), "acc_no" : BankAccount(),..}

3. Bank's records (CLASS)

class Bank {
    total_transaction = 
    total_succsessfull = 
    total_fail = 
    no_of_deposits = 
    total_deposits = 
    total_withdrawls = 
    withdrawls = 
    net_amount =
    today_change = 
    accounts = {"acc_no" : BankAccount}
}

4. Transaction (CLASS)

Transaction {
    transac_id = 
    type =
    source_acc_no = 
    transfered_acc_no = 
    amount = 
}

5. transactions = []

EXECUTION of OPERATIONS

1. Display today's report from raw .csv file

    1. import .csv file to python file
    2. Validate entries
        1. Valid Entries -> Mark as Pass -> Store as objects in Bank -> account {} (if new)
        2. Invalid Entries - Mark as fail
    3. Update dynamic variables of objects of BankAccount and Bank
    4. create a table displpaying today's report of bank:

        total_transaction = 
        total_succsessfull = 
        total_fail = 
        no_of_deposits = 
        total_deposits = 
        total_withdrawls = 
        withdrawls = 
        net_amount =
        today_change = 

2. Display comparison

    1. Import previous and today's transaction .csv files
    2. Check for their overall report .csv file
        1. Exist -> Compare
        2. Not exist -> create -> Compare

    3. Display as table:                        | Previous Day | Today |
        1. Total number of transaction requests |              |
        2. NO. of succesfull transaction        |
        3. No. of failed transaction            |
        4. Total number of deposits             |
        5. Total amount deposits in a day       |
        6. Total no of withdrawls               |
        7. Total withdrawls in a day            |
        8. Net Amount in Bank                   |
        9. Change in a day                      |


3. Create Overall report of the day .csv file

    1. Go through Bank object
    2. Access each element
    3. Put in a list 
    4. Send that list into a .csv file

FUNCTIONS

1. Validate Entries of daily transactions(Bank(), daily_transac.csv)
    
    Correct format - 
        1. deposit/withdraw = [transac_id, type, acc_no, amount]
        2. transfer/receive = [transac_id, type, acc_no, amount, acc_no_from/to]
    
    1. import from .csv to .py file as matrix [[], [], [],...]
    2. Loop through each transaction
    3. Checks type of transaction
    4. Checks standard format of entries
        1. No of data - IndexError
        2. Datatype - ValueError
        3. Amount limit - ValueError
        If fails -> Update Bank's details -> go to next transaction
    5. Checks account number in dictionary (account {})
        1. Exists -> Update Valules
        2. Not exists -> Create object -> send to account {}
    6. Done

2. checks existance of files(filename)
    return True if exists else False

3. create .csv file (Bank())

    Format - [total_transaction, total_succsessfull, total_fail, no_of_deposits, total_deposits, total_withdrawls, withdrawls, net_amount, today_change ]

    1. Loop through each element
    2. store them in a list
    3. Send list to .csv file

4. Create Table(heading[], size_of_cell, data_in_matrix)

    1. Bold Border (=)
    2. Headings
    3. Bold Border (=)
    4. Data in rows
    5. Table Border (=)


Tommorow's worl:-

1. All transaction should have a class\
2. All transactions needs to be collected in a []
3. Define structure of files for this project